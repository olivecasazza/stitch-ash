name = "stitch-ash-web"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# Cloudflare Pages Function bindings are managed in the Pages project settings.
# Do not put placeholder KV ids here; Wrangler validates them during deploy.

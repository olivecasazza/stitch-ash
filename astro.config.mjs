import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  output: 'static',
  // No UI framework integrations — vanilla HTML/CSS for design competition
});
